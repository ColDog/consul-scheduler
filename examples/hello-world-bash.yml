clusters:
  - name: default
    services:
      - bashworld-service

services:
  - name: bashworld-service
    task_name: bashworld
    task_version: 2
    min: 3
    max: 20
    desired: 20

tasks:
  - name: bashworld
    version: 2
    provide_port: true

    containers:
      - name: bashworld
        type: bash
        memory: 1024
        cpu_units: 300
        executor:
          start:
            - echo 'start'
          stop:
            - echo 'stopping'
